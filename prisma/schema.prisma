datasource db {
  provider = "postgresql"
  url      = env("PG_URI")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id        String      @id @default(uuid())
  createdAt DateTime @default(now())
  username  String   @unique
  email     String   
  name      String
  password  String   
  restOwned Resturant[]
  role      Role     @default(user)
}

model Resturant {
  id        String      @id @default(uuid())
  name      String  
  location  String
  ownerId   String
  owner     User        @relation(fields: [ownerId], references: [id]) 
}

enum Role {
  user
  admin
  merchant
}

// Implement a pivot table to make the back relation from the Resturant table to the User table



//model Food {
   //id        String      @id @default(uuid())
  // name      String
 //  price     String
//  hotel     Resturant   @relation(fields: [restId], references: [id])
//}


